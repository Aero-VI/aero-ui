import { AeroButtonProps } from './types';
export declare function AeroButton({ variant, size, loading, disabled, onClick, children, startIcon, endIcon, fullWidth, type, className, ariaLabel, }: AeroButtonProps): import("react/jsx-runtime").JSX.Element;
export default AeroButton;
