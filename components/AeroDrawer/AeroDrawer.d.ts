import { default as React } from '../../../node_modules/react';
import { AeroDrawerProps } from './types';
export declare const AeroDrawer: React.FC<AeroDrawerProps & {
    mobile?: boolean;
}>;
export default AeroDrawer;
